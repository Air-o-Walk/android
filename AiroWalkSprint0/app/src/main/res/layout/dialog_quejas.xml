<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:background="@drawable/bg_panel_green"
        android:gravity="center_horizontal">

        <!-- VOLVER -->
        <TextView
            android:id="@+id/btnVolverQuejas"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:layout_marginStart="4dp"
            android:layout_marginBottom="12dp"
            android:text="← Volver"
            android:textColor="#F1642E"
            android:textSize="15sp" />

        <!-- TITLE -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Quejas / Incidencias"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:textSize="22sp"
            android:layout_marginBottom="12dp" />

        <!-- SUBTEXT -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Describe el problema o incidencia que deseas reportar.\nNuestro equipo lo revisará lo antes posible."
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:gravity="center"
            android:layout_marginBottom="24dp" />

        <!-- TIPO INCIDENCIA -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tipo de incidencia"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:layout_marginBottom="6dp" />

        <Spinner
            android:id="@+id/spinnerTipo"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:spinnerMode="dropdown"
            android:background="@drawable/bg_input"
            android:paddingHorizontal="12dp"
            android:layout_marginBottom="20dp"
            android:contentDescription="Selecciona el tipo de incidencia" />

        <!-- DESCRIPCIÓN -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Descripción"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:layout_marginBottom="6dp" />

        <EditText
            android:id="@+id/editDescripcion"
            android:layout_width="match_parent"
            android:layout_height="160dp"
            android:background="@drawable/bg_input"
            android:gravity="top|start"
            android:padding="16dp"
            android:textColor="#514F77"
            android:textColorHint="#8B86A3"
            android:hint="Describe tu incidencia con el mayor detalle posible"
            android:layout_marginBottom="24dp"
            android:inputType="textMultiLine" />

        <!-- SUBMIT -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnEnviarQueja"
            android:layout_width="160dp"
            android:layout_height="48dp"
            android:text="Enviar"
            android:textColor="#000"
            android:background="@drawable/bg_btn"
            app:backgroundTint="@null"
            app:cornerRadius="40dp" />

    </LinearLayout>
</ScrollView>
